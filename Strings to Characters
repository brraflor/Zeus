// Wastelander_Sage
// Write a method that takes in an array of Strings and returns the number of characters in the array

public class Chars
{
    //used to test the answer
    public static void main(String[]args)
    {
        String[] s = new String[] {"Hello"};    //number of characters is 9
        
        System.out.print(numOfChars(s));
    }
    
    //answer
    public static int numOfChars(String[] s)
    {
        int sum = 0;
        
        for(int i = 0; i < s.length; i++)
        {
            for(int j = 0; j < s[i].length(); j++)
            {
                sum++;
            }
        }
        return sum;
    }
}
